--extra-index-url https://download.pytorch.org/whl/cu126

# Core API & Configuration
google-genai>=2.8.0
python-dotenv>=1.2.2

# Core AI & LLM Ecosystem (GPU Accelerated)
torch
torchvision
torchaudio
sentence-transformers>=3.3.0

# Database Connectivity & Architecture
psycopg2-binary>=2.9.12
pgvector>=0.2.5

# Retrieval, Search & Mathematics
rank-bm25>=0.2.2
numpy>=2.0.0,<2.3.0
scipy>=1.13.0

# Ingestion / PDF Processing
pdf2image>=1.17.0
unstructured>=0.16.0
python-magic-bin>=0.4.14
tqdm>=4.66.0

# Auditing & Report Generation
pandas>=2.2.0
openpyxl>=3.1.0

################################################################################
# WINDOWS PRODUCTION ENVIRONMENT CODES & SETUP CONFIGURATION
################################################################################
# 
# 1. POPPLER DEPLOYMENT (Required for pdf2image processing)
#    - Download compiled Windows binaries from:
#      https://github.com/oschwartz10612/poppler-windows/releases/
#    - Extract ZIP to a secure location (e.g., C:\Program Files\poppler)
#    - Add the bin folder path to your Windows System Environment Variable (PATH):
#      "C:\Program Files\poppler\Library\bin"
#
# 2. LIBMAGIC SYSTEM BINARIES (Resolved)
#    - Handled completely via 'python-magic-bin' directly inside this file.
#      No external DLL configurations are required on Windows.
#
# 3. PSYCOPG2 BINDINGS (Resolved)
#    - Uses 'psycopg2-binary' to ensure seamless interaction with PostgreSQL
#      without requiring local Visual Studio C++ Compilers or pg_config tools.
#
# 4. PYTORCH CUDA OVERRIDE ENGINE
#    - If network drops cause a fallback to CPU builds (+cpu), run:
#      pip uninstall torch torchvision torchaudio -y
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
################################################################################